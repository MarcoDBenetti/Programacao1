#include <stdio.h>
#include <string.h>

int main()
{
    int n;
    char numero_extenso[50];

    printf("Entre com um inteiro de 1 a 99: ");
    scanf("%d", n);

    switch (n) {
        case:
    }

    return 0;
}